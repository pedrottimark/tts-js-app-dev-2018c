import React from 'react';

import BarV from './BarV.js';

const max = 7;
const val = 2;

const SectionBarV = () => (
  <section>
    <BarV max={max} val={val} />
  </section>
);

export default SectionBarV;
