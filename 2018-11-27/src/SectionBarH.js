import React from 'react';

import BarH from './BarH.js';

const max = 7;
const val = 2;

const SectionBarH = () => (
  <section>
    <BarH max={max} val={val} />
  </section>
);

export default SectionBarH;
