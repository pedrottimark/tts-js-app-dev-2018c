const object = {
  mayo: false,
  ketchup: false,
  mustard: true,
  lettuce: true,
  tomato: false,
  pickles: true,
  onion: true,
};

// TODO
// given array of object keys,
// filter to keep keys for which value of property is truthy,
// map to HTML list item with key as text
// join into string with empty string as joiner
const items = ''; // placeholder for result described in comment

console.log(`<ul>${items}</ul>`);
