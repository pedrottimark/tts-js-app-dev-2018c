// Given props object from React,
// return array of attribute objects for HTML.
const toAttributes = props => Object.keys(props).map(
  key => 'TODO'
);

// TODO write your own props object
const props = {

};

console.log(toAttributes(props));
