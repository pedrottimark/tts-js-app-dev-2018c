// Given id as string,
// return product object in products array which has that property value.
// const findProduct = id => products.find(TODO 1);

const onChange = event => {
  const {name, value} = event.target;
  // TODO 2 const description = findProduct(value).description;
  console.info(`onChange name=${name} value=${value}`); // TODO 3 description=${description}
};

// const select = TODO 4;
// select.addEventListener(TODO 5);
